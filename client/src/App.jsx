// Import the landing page component we created.
import LandingPage from "./pages/landingpage";

// This component is the main entry point for our application.
// It determines what content React should display.
function App() {
  // Display the landing page.
  return <LandingPage />;
}

// Export App so the React application can use it.
export default App;