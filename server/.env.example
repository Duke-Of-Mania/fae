# Port used by our Express API.
PORT=5000

# PostgreSQL connection settings.
DB_HOST=
DB_PORT=5432
DB_NAME=
DB_USER=
DB_PASSWORD=

# development | production
NODE_ENV=development

# Origin allowed to make cross-origin requests (the Vite dev server).
CLIENT_ORIGIN=http://localhost:5173
